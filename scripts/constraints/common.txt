# Mirrored from robot-prompt-opt/scripts/constraints/common.txt so both repos resolve the
# same way. See docs/env_parity.md.
#
# Kept even though this repo has no isaaclab (which is what forced it there): several of the
# source builds below (pointnet2_ops, cuRobo, cuTAMP) run setup.py under the same pip, and
# setuptools 81 removed pkg_resources.
setuptools<81
